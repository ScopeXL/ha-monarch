.env
.mm/
__pycache__/
*.pyc
.venv/
venv/

# Claude Code local (personal) settings — keep .claude/settings.json shared
.claude/settings.local.json

# monarch-budget Claude project workspace (reports + the family's financial data)
budget/
